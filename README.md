# BondStats Global Arbitrage Observatory

Public BondStats tool. GitHub Pages serves `index.html`; the scheduled Action updates `data/global-arbitrage-observatory.json` in this same public repository. No cross-repository token is required.

Enable GitHub Pages from the `main` branch / root. The BondStats main site embeds the resulting GitHub Pages URL.
